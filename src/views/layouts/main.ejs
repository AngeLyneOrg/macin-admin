<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> — MACIN Admin</title>

  <!-- Favicon onglet navigateur -->
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/img/icon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/img/icon-16x16.png">

  <!-- Apple Touch Icon (iPhone/iPad — icône écran d'accueil) -->
  <link rel="apple-touch-icon" sizes="180x180" href="/img/apple-touch-icon.png">

  <!-- PWA Manifest (bouton "Installer l'application" dans Chrome) -->
  <link rel="manifest" href="/manifest.json">

  <!-- Meta PWA & thème -->
  <meta name="theme-color" content="#013BFF">
  <meta name="application-name" content="MACIN Admin">
  <meta name="apple-mobile-web-app-title" content="MACIN Admin">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

  <!-- Styles -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <% if (currentUser) { %>
    <div class="app-shell">
      <%- include('../partials/sidebar') %>
      <div class="app-main">
        <%- include('../partials/topbar') %>
        <main class="app-content">
          <%- include('../partials/flash') %>
          <%- body %>
        </main>
      </div>
    </div>
  <% } else { %>
    <main class="auth-content">
      <%- include('../partials/flash') %>
      <%- body %>
    </main>
  <% } %>

  <script src="/js/main.js"></script>
</body>
</html>
